#!/usr/bin/perl -w
#!/usr/bin/perl -w -d:ptkdb
#!


use strict;

# system "content-framework-check";

# if ($?)
# {
#     die "$0: *** Error: content-framework-check says there is something wrong with your documentation workspace.";
# }

system "neurospaces_pull --enable content-framework --regex content-framework --verbose";

if ($?)
{
    die "$0: *** Error: $?";
}
else
{
    system "neurospaces_update --enable content-framework --regex content-framework --verbose";
}
